<?xml version="1.0" encoding="utf-8"?>
<!-- NOTE ONLY. Next bacon: insert as a sibling in
     frameworks/base/packages/SystemUI/res/layout/status_bar_expanded.xml
     inside @id/notification_container_parent, IMMEDIATELY BEFORE
     @id/notification_stack_scroller.

     Do NOT add TalkmanGlassView as a child of NotificationStackScrollLayout.
     NSSL treats children as notification rows.

     Also set res/values/config.xml config_drawNotificationBackground to false
     so NSSL.drawBackground() does not paint a solid round-rect over the glass.
     Do that in the SystemUI patch, not in overlay/ (owned).

     Do not ship by replacing SystemUI.apk on the 2026-09-01 zip.
-->
<com.talkman.glass.TalkmanGlassView
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/talkman_glass_notification_stack"
    android:layout_marginTop="@dimen/notification_panel_margin_top"
    android:layout_width="@dimen/notification_panel_width"
    android:layout_height="match_parent"
    android:layout_gravity="@integer/notification_panel_layout_gravity"
    android:layout_marginBottom="@dimen/close_handle_underlap"
    android:importantForAccessibility="no" />
