#
# Talkman sensor axes. Not LGE p1_global_com.
#
# WOA SEN1 (QCOM2495) AROT / GROT / MROT are identity:
#   "1 0 0" / "0 1 0" / "0 0 1"
# sensors.talkman does not remap. Do not restore LGE 700–902 axis
# swaps or LGE APDS als_ga / prx_cal — those were bullhead/LGE SSC.
#
# Als/prox coefficients live in sensors_hal.c (QPDS-T900 / APDS-9930).
#
# No BMI160 / nanohub axis tables. IMU is ICM-206xx, mag AK09912,
# baro ZPA2326 (same names as sap.conf).
#

:version 0x00000001
:property
