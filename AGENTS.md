# Cursor agent — talkman device tree

This Git repository is `device/msft/talkman` inside `/home/deck/android/los-18.1`.

Open the multi-root workspace: `/home/deck/android/talkman.code-workspace` (Cursor Talkman desktop icon), or this folder.

Rules: `.cursor/rules/talkman.mdc` (always apply).

Do not push `origin` to Android4Lumia950. Author archienz. Boot-only flashes when the owner has a black LK2ND screen.
